// refrence is another name for an object
// it refer's to or points to an object
// it is bind to an object once and once only
// so it MUST be intialized
// oncr it is initialized then all operations on the reference are actually on the object itself

// there can't be a reference to a reference


#include<iostream>

int main()
{ int val = 123;
    int &ref_val = vali;// &ref_val is the reference to val
    

    
    
    }
