#include<ioatream>
#include<math.h>
#include<vector>


using std::cin;
using std::cout;
using std::endl;
using std::vector;


vector <int> tsort (int );

int main(){
    int max;
    cin>>max;
    vector<int> num(max);
    
    vector<int> res = tsort( a, 


}



//for every element x in the input list (unsorted)
// if x > max
//  x=new max and shift all of the array to right
// if x<min 
//  x=new min and append it to the last
// else binery  search for the position
// and 'fit' the new item into that position 

vector<int> tsort(int a,vector<int> b)
{   
    return b    
}
